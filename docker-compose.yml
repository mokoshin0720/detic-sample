version: '3'
services:
  detic-sample:
    environment:
      PYTHONPATH: src/
    build:
      context: .
      dockerfile: Dockerfile
    container_name: 'detic-sample'
    tty: true
    volumes:
      - .:/detic-sample/
